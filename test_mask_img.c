#include "multi_contour.h"

int main(int argc, char * argv[])
{
    if(argc != 2)
    {
        printf("pas assez convainquant, donnez moi de meilleur argument ! \n");
        prinft("./this <fichier img>\n");
        exit(EXIT_FAILURE);
    }
}